// This is a simplified version of the portableTextComponents
// We're not using JSX directly in this file to avoid TypeScript errors
// Instead, we're just exporting a simple object that can be used in the video page

// Empty placeholder for now - we'll handle the rendering directly in the page component
const portableTextComponents = {};

export default portableTextComponents;
